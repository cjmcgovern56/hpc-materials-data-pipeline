# hpc-materials-data-pipeline
Automated materials data extraction and processing pipeline deployed on a Linux remote cluster (Hyak). Features custom Bash shell scripts for SLURM job scheduling and optimized SQL queries for high-dimensional feature extraction.
